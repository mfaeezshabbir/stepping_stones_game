export default function ExamplePlaceholder() {
  return (
    <div className="p-6 text-center">
      <h2 className="text-lg font-semibold">Example removed</h2>
      <p className="text-sm text-muted-foreground mt-2">The WebSocket example was removed when this repository was converted to a frontend-only game. If you need the example, look in the `archive/` folder.</p>
    </div>
  );
}
