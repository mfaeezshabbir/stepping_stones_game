// server.ts (archived)
// This project was converted to frontend-only. The original custom server with Socket.IO
// was archived. Keep this file as a placeholder to indicate the server-side logic was removed.

// If you need to re-enable a custom server, restore the original implementation from
// `archive/server.ts`.

export default undefined;
