// src/lib/db.ts (stub)
// Prisma/database usage was removed when converting the project to frontend-only.
// Keep this stub so any accidental imports won't break the build.

export const db: any = null;