// src/lib/socket.ts (stub)
// Socket.IO usage was removed when the app was converted to frontend-only.
// Keep this stub to prevent build errors from accidental imports.

export const setupSocket = (_io: any) => {
  // no-op
};